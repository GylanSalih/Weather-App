import { ReactElement } from 'react';
import './comparison.module.scss';

export const Comparison = (): ReactElement => {
  return (
    <div>
      <h1>Comparison Page mit LandingImg und Vergleichskomponente</h1>
    </div>
  );
};
